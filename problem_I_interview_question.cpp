#include <iostream>
using namespace std;

// Buzz Words
// Fizz - if i%A == 0
// Buzz - if i%B == 0
// FizzBuzz - if i%B == 0 && if i%A == 0

int main(){
    int start, end;
    cin >> start >> end;
    for(int i=start; i<=end; i++){
        cin >> 
    }
    return 0;
}
